# Dependencias
node_modules/

# Build
dist/
dist-ssr/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / SO
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/
*.local
.obsidian/
docs/.obsidian/

# Vite
.vite/

# Variables de entorno — solo se versiona `.env.example`.
.env
.env.*
!.env.example

# TypeScript — caché incremental (se regenera con `tsc -b`, no se versiona)
*.tsbuildinfo

# Playwright — artefactos de pruebas E2E
playwright-report/
test-results/
blob-report/

# Sesiones y artefactos privados de la auditoría manual de la web oficial
# (nunca deben versionarse: pueden contener cookies, tokens o capturas
# con datos personales de la cuenta de prueba)
playwright/.auth/
.auth/
audit-private/
audit-logs-private/
audit-temp/
storageState*.json
*.storage-state.json
*.session.json
